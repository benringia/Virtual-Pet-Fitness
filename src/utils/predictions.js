// Returns 30-day projections based on recent averages.
// Stub — to be implemented once calHistory and workouts have real data.
export function computePredictions(_state) {
  return {
    calories: null,
    workouts: null,
    xp: null,
  }
}
